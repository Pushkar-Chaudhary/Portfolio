{
  "framework": "vite",
  "buildCommand": "npm --prefix portfolio install && npm --prefix portfolio run build",
  "outputDirectory": "portfolio/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
